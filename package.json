{
  "name": "gamexo",
  "private": true,
  "packageManager": "pnpm@11.5.1",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "generate:api": "turbo run generate:api",
    "deploy:build": "./deploy/build.sh",
    "deploy:cf": "pnpm deploy:build && wrangler deploy --config deploy/wrangler.jsonc",
    "docs:dev": "pnpm --filter @gamexo/docs dev",
    "docs:build": "pnpm --filter @gamexo/docs build",
    "docs:deploy": "pnpm docs:build && wrangler deploy --config deploy/wrangler.docs.jsonc"
  },
  "devDependencies": {
    "turbo": "^2.5.6",
    "wrangler": "^4.120.0"
  }
}
