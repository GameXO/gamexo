export const appName = 'gamexo developers';
export const docsRoute = '/docs';
export const docsImageRoute = '/og/docs';
export const docsContentRoute = '/llms.mdx/docs';

export const gitConfig = {
  user: 'GameXO',
  repo: 'gamexo',
  branch: 'main',
};

/**
 * The base URL shown in every example.
 *
 * Illustrative on purpose. A partner's real base URL is issued with their API key,
 * because it depends on the deployment they are pointed at — and hard-coding today's
 * host into published documentation is how a partner ends up integrating against a
 * URL we have since moved off.
 */
export const apiBaseUrl = 'https://api.gamexo.app';
